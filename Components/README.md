# Components

This directory contains ORB-SLAM-maintained code snapshots that are built as part of this repository.

`DBoW2`, `g2o`, and `Sophus` are not treated as replaceable upstream dependencies here. They are kept in-tree because ORB-SLAM uses modified or compatibility-pinned versions of these projects.
